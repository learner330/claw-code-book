# 写作风格指南（第二阶段：社区扩展模块）

本文件是 claw-code-book 第二阶段的写作规则，覆盖核心运行时之外的社区扩展模块。所有第二阶段章节的生成和重写都必须遵循此指南。

第一阶段指南（writing-guide.md）中的语言风格、代码引用、图表规则仍然有效，除非本文件明确覆盖或调整。

---

## 一、第二阶段的目标与范围

### 什么是社区扩展模块

社区扩展模块指不在核心 Agent 运行时链路中、具备独立生命周期、面向特定场景或作为示例/参考存在的组件。具体包括：

| 模块 | 类型 | 所在目录 | 说明 |
|------|------|----------|------|
| claw-analog | 独立工具 | rust/crates/claw-analog/ | 精简版 agent harness，窄工具集，面向 CI/自动化 |
| claw-rag-service | 独立服务 | rust/crates/claw-rag-service/ | 工作区 RAG 索引与检索服务 |
| telemetry | 共享库 | rust/crates/telemetry/ | 会话追踪、事件收集、遥测 sink |
| Python 原始实现 | 兼容/历史层 | src/ | Rust 重写前的 Python 移植层，含命令图、查询引擎等 |
| 插件示例 | 示例代码 | rust/crates/plugins/bundled/ | plugin.json 结构与 hook 脚本示例 |
| 容器化支持 | 部署设施 | Containerfile, docker-compose.yml, docs/container.md | Docker/Podman 工作流与沙箱检测 |
| 周边文档 | 参考文档 | docs/ | 本地模型接入、个人助手路线图等 |

### 与第一阶段的核心区别

| 维度 | 第一阶段（核心运行时） | 第二阶段（社区扩展） |
|------|----------------------|---------------------|
| 讲解重心 | 源码实现与设计决策 | 模块边界、使用方式、与核心的关系 |
| 代码比例 | 源码分析占 80% | 使用示例 + 源码关键片段各占 50% |
| 语言范围 | 仅 Rust | Rust + Python + Shell/Docker |
| 章节独立性 | 强依赖前一章（按数据流顺序） | 各章可独立阅读 |
| 目标读者 | 想理解 Agent 架构的工程师 | 想扩展、部署或对接系统的工程师 |

---

## 二、语言风格：继承与调整

### 继承第一阶段的规则

1. **禁止反问句开场、感叹式过渡、逐段总结、滥用加粗、emoji、拟人叙事**。
2. **用陈述句，代码优先，一段只说一件事，语气中性**。
3. **图表用 mermaid 和表格，禁止装饰性图片**。

### 第二阶段新增规则

1. **先讲"能做什么"，再讲"怎么实现"**。独立工具的章节开篇必须说明该模块解决什么场景问题，再进入内部结构。
2. **与核心系统的边界必须显式说明**。每章至少有一处明确指出：该模块与核心运行时如何交互（HTTP/函数调用/共享库/无交互）。
3. **历史语境允许使用**。讲解 Python 原始实现时，可以说明"Rust 重写前的实现方式是 X，当前核心运行时改为 Y"，帮助读者理解演进关系。
4. **配置项和 CLI 参数用表格汇总**。独立工具涉及大量 flags/env/config，必须用表格集中列出，不要散落在正文中。

---

## 三、多语言代码引用规则

第二阶段涉及 Rust、Python、Shell、Dockerfile、TOML、JSON 等多种语言，统一规则如下：

### 1. 源码位置标注格式

```markdown
// claw-code/rust/crates/claw-analog/src/lib.rs
```

```markdown
// claw-code/src/runtime.py
```

```markdown
// claw-code/Containerfile
```

### 2. 截取策略

- **Rust 源码**：与第一阶段相同，截取 10-25 行关键片段。
- **Python 源码**：同样截取 10-25 行。Python 代码常含类型注解和 docstring，允许保留函数签名级别的完整性。
- **Shell/Dockerfile/配置**：允许完整引用短小文件（如 plugin.json），但超过 30 行的仍需截取。

### 3. 代码块语言标注

必须精确标注语言类型，便于 VitePress 高亮：

| 内容类型 | 标注 |
|----------|------|
| Rust | rust |
| Python | python |
| Shell 脚本 / Bash | bash |
| PowerShell | powershell |
| Dockerfile | dockerfile |
| TOML | toml |
| JSON | json |
| 终端输出 / 文本日志 | text |

### 4. 多语言对比场景

当需要对比 Rust 与 Python 的同一功能时，使用相邻的两个代码块，前导说明用一句话点明对比点。不要混在一个代码块里。

---

## 四、独立工具的章节结构

claw-analog 和 claw-rag-service 属于独立工具/服务，其章节结构不同于核心模块：

```markdown
# 第N章 标题：副标题

## 本章概览（总）

这章讲什么（1-2句），面向什么场景，与核心运行时的关系。
关键文件清单（表格：文件路径 → 职责）。

## N.1 设计理念与使用场景（分）

为什么需要这个工具，它解决了核心运行时没覆盖的什么问题。
与核心 CLI（claw）的功能对比（表格）。

## N.2 配置与 CLI 参数（分）

环境变量表格、CLI flags 表格、配置文件示例（TOML/JSON）。

## N.3 核心实现（分）

关键源码片段 + 设计分析。
如果是服务，包含 HTTP API 路由表。

## N.4 与核心系统的集成（分）

数据流/调用链：该模块如何与核心运行时交互。
用 mermaid 图表示。

## 小结（总）

3-5句，列出关键文件、核心机制、适用场景。
与下一章的衔接（1句）。
```

---

## 五、Python 原始实现的特殊规则

Python 原始实现（src/ 目录）是 Rust 重写的参考层，不是当前运行时代码。讲解时需遵循：

1. **明确标注"Python 原始实现"**。代码块前一行写明 `// claw-code/src/xxx.py`，正文中用"Python 原始实现中的 X"来指代，避免读者误以为这是当前运行时代码。
2. **讲解目的限定为"架构对照"**。说明 Python 层如何组织命令图、工具池、查询引擎，以及 Rust 重写时如何拆分/迁移到哪些 crate。
3. **不逐行分析 Python 代码**。只选取能说明架构设计的片段（如 bootstrap_graph.py 的阶段定义、runtime.py 的 session 结构）。
4. **可以引用 reference_data/**。src/reference_data/ 下的 JSON 文件是子系统的镜像快照，适合用来说明模块划分。

---

## 六、插件示例与部署设施的规则

### 插件示例（plugins/bundled/）

- 按目录结构逐层说明：plugin.json 的 schema、hooks/ 脚本的触发时机、与核心 plugins crate 的对应关系。
- plugin.json 完整引用（通常不超过 20 行），因为这是契约定义。
- hooks 脚本引用关键片段即可。

### 容器化支持

- 以设计意图和架构决策为主，不是源码分析。
- Containerfile 和 docker-compose.yml 允许完整引用，或截取说明设计思路的关键片段。
- 可引用配置片段帮助读者理解容器与原系统的对接方式，不强制要求提供可运行的完整命令。

---

## 七、章节标题规范

与第一阶段相同：每章标题包含主标题和副标题，格式为 `第N章 主标题：副标题`。

第二阶段的副标题建议包含模块类型标识，帮助读者快速判断：

- 独立工具：`claw-analog：精简 Agent Harness`
- 独立服务：`claw-rag-service：RAG 检索服务`
- 兼容层：`Python 原始实现：移植层架构`
- 示例/生态：`插件示例与契约：从 plugin.json 到生命周期`
- 部署设施：`容器化与部署：Docker/Podman 工作流`

---

## 八、与第一阶段的衔接

第二阶段各章的开篇或小结中，应当建立与第一阶段已讲内容的关联：

- 引用第一阶段已覆盖的核心模块时，直接写"第 X 章介绍的 Y 组件"。
- 不要重复讲解核心模块的内部机制，只说明接口层面的交互关系。
- 如果某个社区扩展模块尚未稳定（如文档中标注"实验性"或"将来可能迁移"），允许在文中说明其状态。
